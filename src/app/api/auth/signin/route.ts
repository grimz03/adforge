import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {db} from '@/lib/db';import {createSession} from '@/lib/auth';
export async function POST(req:Request){try{const b=await req.json(),email=String(b.email||'').trim().toLowerCase(),password=String(b.password||''),user=await db.user.findUnique({where:{email}});if(!user||!(await bcrypt.compare(password,user.passwordHash)))return NextResponse.json({error:'Email or password is incorrect.'},{status:401});await createSession(user.id);return NextResponse.json({user:{id:user.id,name:user.name,email:user.email}})}catch{return NextResponse.json({error:'Unable to sign in right now.'},{status:500})}}
